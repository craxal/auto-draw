let MyNum = (3 + 4) * 5;
let MyColor = #123456;
let MyAngle = 120deg;
// let MyComment = 0deg;
let MyFunction = fn (a, b, c) {
    ArcLeft(180, 30);
    if MyNum > 4 or MyNum <= 5 {
        PenUp();
    }
};
{}
{
    var a = 1;
    var b = 2;
    if a < b {
        var c = 3;
    } else {
        var d = 4;
    }
    b = 5;
    // DoSomething;
}
while false {
    // DoSomethingElse;
}
PenColor(#123456);
PenDown();
MyFunction(1, 2, 3);
ArcLeft(180deg, 30);

let MakeCounter = fn () {
  var i = 0;
  let Count = fn () {
    i = i + 1;
    Print(i);
    return i;
  };

  return Count;
};
var counter = MakeCounter();

Print(1);

counter();
counter();
